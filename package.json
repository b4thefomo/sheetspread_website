{
  "name": "sailsmaps-blog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "create-post": "node scripts/createNextPost.js",
    "sanitize": "node scripts/sanitizeContent.js",
    "remove-images": "node scripts/removeImageTags.js",
    "generate-image": "node scripts/generateImage.js",
    "generate-specific": "node scripts/generateSpecificImages.js",
    "post-process": "node scripts/postProcessBlog.js",
    "test-automation": "node -e \"console.log('Testing automation setup...'); const fs = require('fs'); const cal = JSON.parse(fs.readFileSync('./content-calendar.json')); const next = cal.posts.find(p => p.status !== 'Done'); console.log(next ? '✅ Next post to generate: ' + next.id : '⚠️  No posts to generate');\""
  },
  "dependencies": {
    "@google/genai": "^1.16.0",
    "@google/generative-ai": "^0.24.1",
    "@tailwindcss/typography": "^0.5.16",
    "@vercel/analytics": "^1.5.0",
    "axios": "^1.11.0",
    "dotenv": "^17.2.1",
    "gray-matter": "^4.0.3",
    "next": "14.0.0",
    "react": "^18",
    "react-dom": "^18",
    "sharp": "^0.33.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10.0.1",
    "eslint": "^8",
    "eslint-config-next": "14.0.0",
    "postcss": "^8",
    "tailwindcss": "^3.3.0",
    "typescript": "^5"
  }
}
